import facebookLogo from "../images/facelogo.png";

const Header = (props) => {
  return (
    <div>
      <img alt="facebookLogo" src={facebookLogo}></img>
    </div>
  );
};

export default Header;
